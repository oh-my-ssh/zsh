# install zsh if not yet available

command -v /opt/bin/zsh >/dev/null 2>&1 || { 
    docker run --rm -v /opt/bin:/target ohmyssh/zsh
    docker rmi ohmyssh/zsh 
}

export ZDOTDIR=$SSHHOME/.sshrc.d/

if [ -t 1 ]; then
    exec /opt/bin/zsh
fi
